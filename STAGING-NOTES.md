# Occono staging notes

- The staging site is intentionally `noindex` and `robots.txt` blocks crawlers.
- Replace staging canonical/OG URLs with `https://occono.co.uk/` at launch.
- The contact form currently opens the visitor’s email app addressed to `hello@occono.co.uk`.
- Once Google Workspace is active, connect a form endpoint (Formspree, Web3Forms, or a Vercel/Cloudflare serverless function) for direct delivery and spam filtering.
- Review legal drafts and insert final business identity/address details before commercial launch.
